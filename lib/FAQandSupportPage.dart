import 'package:flutter/material.dart';

class FAQandSupportPage extends StatelessWidget {
  const FAQandSupportPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("FAQ & Support"),
      ),
    );
  }
}
